import 'package:flutter/material.dart';

class ArbeitnehmerProfilPage extends StatelessWidget {
  const ArbeitnehmerProfilPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Arbeitnehmer Profil'),
      ),
    );
  }
}
