import 'package:flutter/material.dart';

class ArbeitgeberProfilPage extends StatelessWidget {
  const ArbeitgeberProfilPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'Arbeitgeber Profil',
        ),
      ),
    );
  }
}
